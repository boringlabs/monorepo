{
  "name": "boring-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "graph": "graph deploy --node https://api.studio.thegraph.com/deploy/ boring",
    "create-local": "graph create --node http://localhost:8020/ boring",
    "remove-local": "graph remove --node http://localhost:8020/ boring",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 boring",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.69.1",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
